# Лана: автоцикл, сводка 12:00, модули, приписка — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Лана обрабатывает письма о возвратах полностью автономно и молча, шлёт одну сводку в 12:00 (новые / сроки / нужно решение), сверяет модули по трём источникам и не ломает вёрстку приписки про сертификаты.

**Architecture:** Два артефакта: (1) скилл `roles/client-office-ops/skills/refund-request/SKILL.md` в этом репо (на сервер попадает через git pull клона `/home/agentbot/gogol-school-ai`); (2) `/home/agentbot/bots/lana/bot.py` на ozma — промпты оркестратора, PERSONA, allowlist. bot.py правится локально (scp туда-сюда), не через sed.

**Tech Stack:** Markdown-скилл, Python (строковые константы в bot.py), ssh/scp на root@ozma.gogol.school, systemd `lana-bot`.

## Global Constraints

- Спека: `docs/superpowers/specs/2026-07-24-lana-improvements-design.md` — источник требований.
- Железное правило №1 (только черновики, никакой отправки) НЕ трогать нигде.
- Терминальный статус «деньги возвращены» Лана не ставит никогда — сохранить во всех правках.
- `.claude/skills/refund-request` — симлинк на `roles/client-office-ops/skills/refund-request`: правится ОДИН файл.
- Бэкап bot.py уже есть: `bot.py.bak-20260724` (там же добавлен `--model opus` — не потерять при правках).
- В чате Telegram: блоки через `---` на отдельной строке = отдельные сообщения; ответ ровно `NOOP` в чат не уходит.
- Все правки bot.py: после изменения `python3 -m py_compile bot.py` обязателен до рестарта.

---

### Task 1: SKILL.md — автономный режим (шаги 5–6)

**Files:**
- Modify: `roles/client-office-ops/skills/refund-request/SKILL.md` (раздел «## Шаг 5 — сводка и подтверждение (ничего не пишем до «да»)» и начало «## Шаг 6»)

**Interfaces:**
- Produces: понятие «автономный прогон» и «типовой/нестандартный кейс», на которые ссылаются Task 2 и новые промпты bot.py (Task 5–6). Формулировки ниже — канонические.

- [x] **Step 1: Прочитать текущий текст шагов 5–6** — `Read` файла, найти заголовок шага 5.

- [x] **Step 2: Вставить блок про режимы сразу ПОСЛЕ заголовка «## Шаг 5 — сводка и подтверждение (ничего не пишем до «да»)»:**

```markdown
> **Режимы работы (с 24.07.2026).**
> **Автономный прогон** (плановый разбор почты по расписанию, в промпте сказано «автозапуск»): шаг 5 ПРОПУСКАЕТСЯ — сводка в чат не пишется, подтверждение не запрашивается. Типовой кейс исполняется по шагу 6 сразу; правило «показать текст письма оператору до создания черновика» в этом режиме не действует — контроль человека происходит при проверке черновика в Drafts перед отправкой.
> **Типовой кейс** = студент и заявка найдены однозначно, программа и оплата понятны, расчёт сумм сходится по всем источникам (шаг 4). **Нестандартный кейс** = любой из списка «Нестандартные кейсы» ниже, а также любое расхождение в модулях/расчёте — в автономном прогоне по нему НИЧЕГО не записывается, письмо остаётся непрочитанным и попадает в сводку 12:00 блоком «нужно решение».
> **Идемпотентность** (обязательна в автономном режиме — человека-подтверждателя в петле нет): перед каждой записью проверить, что уже сделано — есть ли нетерминальная строка в Notion по этой почте, стоят ли статусы в заявке, есть ли черновик в цепочке. Делать только недостающие шаги, дублей не плодить. `mark_read` — только после полного успешного цикла; при ошибке на полпути письмо оставить непрочитанным (кейс уйдёт в «нужно решение»).
> **Интерактивный режим** (человека попросили в чате или скилл запущен вручную): прежний порядок — шаг 5 со сводкой и ожиданием «да» полностью в силе.
```

- [x] **Step 3: Проверить** — `grep -c "Автономный прогон" SKILL.md` → 1; `grep -n "Режимы работы" SKILL.md` → внутри шага 5.

- [x] **Step 4: Commit**

```bash
git add roles/client-office-ops/skills/refund-request/SKILL.md
git commit -m "refund-request: автономный режим — шаг 5 пропускается, идемпотентность, типовой/нестандартный кейс"
```

---

### Task 2: SKILL.md — модули из трёх источников (шаг 4)

**Files:**
- Modify: `roles/client-office-ops/skills/refund-request/SKILL.md` — раздел «## Шаг 4», подраздел про модульную сетку (абзац «Модульная сетка (доли всегда 30% / 50% / 20%, отличается размер модулей):»)

**Interfaces:**
- Consumes: термин «нестандартный кейс» из Task 1.
- Produces: правило «модули из трёх источников», на которое ссылается новый ORCH_MAIL_PROMPT (Task 5). Тул `mcp__gogol-school-site__iblock_element_get` добавляется в allowlist в Task 6.

- [x] **Step 1: Вставить ПЕРЕД абзацем «Модульная сетка (доли всегда 30% / 50% / 20%…)» блок:**

```markdown
**Границы модулей — сверять по ТРЁМ источникам, ничего не выдумывать** (анти-кейс Лебедевой 17.07.2026: ЛАБ-2 оказалась на 16 занятий, а не 24 — «М1=2 по памятке» была выдумана):

1. **Число занятий и «прошло»** — только по дочерним занятиям продукта в Озме (даты занятий vs дата письма). Число занятий в названии/описании — вторично.
2. **Описание продукта в карточке Озмы** (`crm.actions`, поле описания) — модули прописываются там при заведении.
3. **Страница расписания сайта** — Bitrix iblock 38, элемент 27918, читать `mcp__gogol-school-site__iblock_element_get`; найти программу по коду/названию в HTML и взять её модули.
4. Общая сетка ниже — только проверка/fallback, если в п. 2–3 модулей нет.

Все источники сходятся → считать по формуле и сверить результат с `auto_refund_amount` карточки. **Любое расхождение** (источники между собой, расчёт ≠ `auto_refund_amount`, программа не найдена ни в описании, ни на /schedule/) → кейс НЕСТАНДАРТНЫЙ: черновик с суммой не создавать, суммы клиенту не называть; в сводку/оператору — цифры по каждому источнику и в чём разошлись.
```

- [x] **Step 2: Проверить** — `grep -c "трём источникам\|ТРЁМ источникам" SKILL.md` ≥ 1; `grep -c "iblock_element_get" SKILL.md` = 1.

- [x] **Step 3: Commit**

```bash
git add roles/client-office-ops/skills/refund-request/SKILL.md
git commit -m "refund-request: модули из 3 источников (занятия Озмы, описание продукта, /schedule/), расхождение = нестандарт"
```

---

### Task 3: SKILL.md — вёрстка приписки про сертификаты (шаг 6)

**Files:**
- Modify: `roles/client-office-ops/skills/refund-request/SKILL.md` — шаг 6, пункт 3 «Черновик ответа» (после строки про TOV/подпись)

**Interfaces:**
- Consumes: ничего. Produces: правило вёрстки для черновиков (используется в автономном и интерактивном режимах одинаково).

- [x] **Step 1: Проверить, поддерживает ли create_draft HTML** — посмотреть схему тула на сервере MCP:

```bash
ssh root@ozma.gogol.school 'grep -n -A5 "def create_draft\|body_html\|html" ~/YandexMailMCP/*.py 2>/dev/null | head -30'
```

Если параметра HTML нет — правило ниже остаётся в plain-text варианте (оно так и написано); если есть — добавить в конец вставки строку «Если доступно HTML-тело — вставлять блок правил HTML-фрагментом шаблона без изменений.»

- [x] **Step 2: Вставить в шаг 6, в пункт про черновик (после строки «TOV по shared/marketing.md…») подпункт:**

```markdown
   - **Вёрстка нижнего блока правил (обязательно, фидбэк 24.07.2026):** блоки «Правила частичного возврата» / «Правила перевода в сертификат» и сноску `*Использование сертификата…` переносить из шаблона КАК ЕСТЬ, не пересобирать и не сжимать. В итоговом теле письма: перед сноской `*Использование…` — пустая строка, сноска отдельным абзацем; три следствия («присвоению сертификата статуса…», «прекращению обязательств…», «признанию денежных средств…») — маркированным списком, каждый пункт с новой строки с дефисом; никаких `&nbsp;` и слипшихся в одну строку абзацев. После сборки черновика перечитать нижний блок глазами получателя: если абзацы слиплись — переверстать до создания черновика.
```

- [x] **Step 3: Проверить** — `grep -c "Вёрстка нижнего блока" SKILL.md` = 1.

- [x] **Step 4: Commit + push обоих скилл-коммитов**

```bash
git add roles/client-office-ops/skills/refund-request/SKILL.md
git commit -m "refund-request: вёрстка приписки про сертификаты — блок из шаблона как есть, сноска и следствия с переносами"
git push
```

---

### Task 4: git pull на сервере

**Files:**
- Server: `/home/agentbot/gogol-school-ai` (клон, cwd прогонов Ланы)

- [x] **Step 1: Pull**

```bash
ssh root@ozma.gogol.school 'cd /home/agentbot/gogol-school-ai && sudo -u agentbot git pull --ff-only'
```

Expected: подтянулись 3 коммита скилла (+ коммиты спеки/плана).

- [x] **Step 2: Проверить** — `ssh root@ozma.gogol.school 'grep -c "Автономный прогон" /home/agentbot/gogol-school-ai/roles/client-office-ops/skills/refund-request/SKILL.md'` → 1.

---

### Task 5: bot.py — ORCH_MAIL_PROMPT (автоцикл + тишина) и ORCH_DIGEST_PROMPT (сводка 12:00)

**Files:**
- Modify: `/home/agentbot/bots/lana/bot.py` (константы `ORCH_MAIL_PROMPT`, `ORCH_DIGEST_PROMPT`, `ORCH_DIGEST_HOUR`)
- Рабочий процесс: `scp root@ozma.gogol.school:/home/agentbot/bots/lana/bot.py <scratchpad>/bot.py` → править локально Edit-тулом → `scp` обратно.

**Interfaces:**
- Consumes: термины «автономный прогон», «типовой/нестандартный кейс», «модули из трёх источников» из Task 1–2 (скилл уже на сервере после Task 4).
- Produces: поведение, которое проверяет Task 7.

- [x] **Step 1: Скачать bot.py локально** (`scp`), убедиться что в нём уже есть `"--model", "opus"`.

- [x] **Step 2: Заменить значение `ORCH_MAIL_PROMPT` целиком на:**

```python
ORCH_MAIL_PROMPT = (
    "(Автозапуск по расписанию — плановый разбор почты. РЕЖИМ АВТОНОМНЫЙ, в чат ты НИЧЕГО не пишешь.) "
    "Разбери НОВЫЕ письма в ящике возвратов по скиллу refund-request: только непрочитанные, "
    "пришедшие за последние 2 суток; старый бэклог не трогай. "
    "ТИПОВОЙ кейс (студент и заявка найдены однозначно, программа и оплата понятны, расчёт сходится "
    "по всем источникам из шага 4 скилла) — выполняй весь цикл СРАЗУ, без подтверждения: статусы в заявке "
    "(«Отмена» + «Попросил возврат», при выборе сертификата — «Перевод в сертификат»), строка в таблице "
    "возвратов Notion со статусом «написать», черновик ответа в Drafts (reply в цепочку), затем mark_read. "
    "ИДЕМПОТЕНТНОСТЬ: перед каждой записью проверь, что уже сделано (нетерминальная строка в Notion по этой "
    "почте, уже стоящие статусы, черновик в цепочке) — делай только недостающее, дублей не создавай. "
    "mark_read — только после полного успешного цикла; ошибка на полпути → письмо оставить непрочитанным. "
    "НЕСТАНДАРТНЫЙ кейс (список в скилле: спор о сумме, отмена школой, студент не найден или несколько "
    "кандидатов, расхождение модулей или расчёта, реквизиты письмом без анкеты и т.п.) — НИЧЕГО не записывай "
    "и письмо не читай: он попадёт в сводку 12:00 блоком «нужно решение». "
    "В чат не пиши ни при каком исходе — в конце ответь ровно одним словом NOOP и ничем больше."
)
```

- [x] **Step 3: Заменить `ORCH_DIGEST_HOUR = 10` на `ORCH_DIGEST_HOUR = 12` (комментарий: `# сводка дня в 12:00`) и значение `ORCH_DIGEST_PROMPT` целиком на:**

```python
ORCH_DIGEST_PROMPT = (
    "(Автозапуск: сводка дня в 12:00, будний день.) Сформируй сводку по возвратам отдельными сообщениями — "
    "блоки разделяй строкой --- на отдельной строке; пустые блоки полностью пропускай. "
    "БЛОК 1 «✉️ Новые за сутки»: строки таблицы возвратов Notion, созданные с прошлой сводки (за последние "
    "сутки; в понедельник — с вечера пятницы): по каждой — ФИО, почта, программа, сумма, ссылка на строку. "
    "В конце блока: «Черновики в Drafts — проверить и отправить». "
    "БЛОК 2 «⏰ Сроки»: нетерминальные строки таблицы с заполненным Дедлайном: 🔴 дедлайн прошёл или сегодня, "
    "🟡 ближайшие 1–3 дня — по каждой ФИО, почта, статус, дата. Плюс зависшие по SLA: «написать» больше 1 дня, "
    "«запросили доки» больше 7 дней, «заявление на согласовании» больше 2 дней, «на подписи» больше 7 дней. "
    "БЛОК 3 «⚠️ Нужно решение» с тегом @Beverkar2: непрочитанные письма ящика, отложенные как нестандартные "
    "(и всё, что ты не смогла оформить автоматически): по каждому — кто, что случилось, что нужно решить; "
    "при споре о сумме — цифры по каждому источнику (занятия Озмы, описание продукта, /schedule/, "
    "auto_refund_amount). "
    "В этом прогоне ничего не записывай в CRM/Notion без «да» в чате. "
    "Если все блоки пусты — ответь ровно NOOP."
)
```

- [x] **Step 4: Обновить комментарий у `ORCH_THREAD = 3`** с «топик «Новые письма»…» на «топик «Сводки» (бывш. «Новые письма») — сводка 12:00 и «нужно решение»».

- [x] **Step 5: Залить назад и проверить синтаксис**

```bash
scp <scratchpad>/bot.py root@ozma.gogol.school:/home/agentbot/bots/lana/bot.py
ssh root@ozma.gogol.school 'cd /home/agentbot/bots/lana && python3 -m py_compile bot.py && echo OK && grep -c "РЕЖИМ АВТОНОМНЫЙ" bot.py && grep -n "ORCH_DIGEST_HOUR" bot.py'
```

Expected: `OK`, счётчик 1, `ORCH_DIGEST_HOUR = 12`.

---

### Task 6: bot.py — PERSONA (правило №2) и allowlist

**Files:**
- Modify: тот же локальный `<scratchpad>/bot.py` (можно объединить с Task 5 в одну заливку — тогда Step 5 Task 5 выполняется после этого таска)

- [x] **Step 1: В PERSONA заменить текст правила №2.** Старый фрагмент (внутри строки):

```
ЖЕЛЕЗНОЕ ПРАВИЛО №2: перед ЛЮБОЙ записью (статусы в CRM, строки в Notion, карточка возврата, черновик письма с суммой) сначала простыми словами опиши, что собираешься сделать и какая сумма, и дождись явного «да»/«ок» в чате. Терминальный статус «деньги возвращены» не ставишь никогда.
```

Новый:

```
ЖЕЛЕЗНОЕ ПРАВИЛО №2: в ПЛАНОВОМ автозапуске разбора почты типовой кейс входящего письма о возврате ты оформляешь сама, без подтверждения (статусы заявки, строка в Notion, черновик — по скиллу, идемпотентно). Во ВСЕХ остальных случаях (просьбы в чате, скилл refund, нестандартные кейсы, любые правки вне конвейера письма) перед записью сначала простыми словами опиши, что собираешься сделать и какая сумма, и дождись явного «да»/«ок» в чате. Терминальный статус «деньги возвращены» не ставишь никогда.
```

- [x] **Step 2: В ALLOWED_TOOLS в блок «Сайт (Bitrix)» добавить строку** после `"mcp__gogol-school-site__users_orders",`:

```python
    "mcp__gogol-school-site__iblock_element_get",   # страница /schedule/ (iblock 38, эл. 27918) — модули программ
```

- [x] **Step 3: Залить, py_compile, проверить**

```bash
ssh root@ozma.gogol.school 'cd /home/agentbot/bots/lana && python3 -m py_compile bot.py && grep -c "iblock_element_get" bot.py && grep -c "ПЛАНОВОМ автозапуске" bot.py'
```

Expected: оба счётчика = 1.

---

### Task 7: Рестарт, smoke-тест, наблюдение

- [x] **Step 1: Рестарт**

```bash
ssh root@ozma.gogol.school 'systemctl restart lana-bot && sleep 3 && systemctl is-active lana-bot && journalctl -u lana-bot -n 5 --no-pager -q'
```

Expected: `active`, строка «Lana Refund bot started».

- [x] **Step 2: Smoke сводки** — одноразовый запуск дайджеста через тест-хук:

```bash
ssh root@ozma.gogol.school 'systemctl stop lana-bot && cd /home/agentbot/bots/lana && sudo -u agentbot ORCH_RUN_ON_START=digest timeout 900 python3 bot.py & sleep 600; ssh root@ozma.gogol.school "pkill -f \"ORCH_RUN_ON_START\"; systemctl start lana-bot"'
```

(Практично проще: выставить env в юните нельзя — запустить руками, дождаться сообщения в топике «Сводки», убить, вернуть сервис. Альтернатива без остановки: дождаться штатного прогона в 12:00 следующего буднего дня.) Проверить в чате: три блока (или меньше, пустые опущены), формат ФИО+почта, 🔴/🟡.

- [ ] **Step 3: Наблюдение почасового прогона** (первый прогон 16:05 24.07 — на контроле) — в ближайший час `journalctl -u lana-bot -f`: прогон mail завершился NOOP (в чат ничего), в ящике обработанные письма прочитаны, в Notion появились строки, в Drafts черновики. Если письмо типовое, а Лана всё же спросила «Делаю?» — правило №2/промпт не сработали, вернуться к формулировкам.

- [x] **Step 4: Обновить память** — в `reference_lana_bot.md`: дайджест теперь 12:00, mail-прогоны молчат (NOOP всегда), правило №2 смягчено для конвейера, allowlist +iblock_element_get, топик 3 переименован в «Сводки» (когда Бэлла даст право).

- [x] **Step 5: Commit плана с отмеченными чекбоксами** (`git add docs/superpowers/plans/… && git commit`).

## Self-Review

- Покрытие спеки: §1 тишина+автоцикл → Task 1, 5, 6; §2 сводка 12:00 → Task 5; §3 модули → Task 2, 6; §4 приписка → Task 3; деплой/приёмка → Task 4, 7. Переименование топика — вне плана (ждёт права от Бэллы, делается одним API-вызовом).
- Типы/имена: `ORCH_MAIL_PROMPT`/`ORCH_DIGEST_PROMPT`/`ORCH_DIGEST_HOUR`/`ALLOWED_TOOLS`/`PERSONA` — как в текущем bot.py; тул `mcp__gogol-school-site__iblock_element_get` одинаков в Task 2 и Task 6.
- Плейсхолдеров нет; единственная развилка — HTML в create_draft (Task 3 Step 1 решает её по факту).
