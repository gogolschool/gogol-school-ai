# Скилл /b2b — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Скилл `/b2b` для раздела «Корпоративные клиенты» OzmaDB: заведение клиентов/сделок/продуктов/действий, дописывание информации, отчёты, голосовые, контекст «это Вика».

**Architecture:** Тонкий `SKILL.md` в git → мастер-промпт в Notion («База знаний», рядом с Дамблдором). Расшифровка аудио — `scripts/transcribe.py` на faster-whisper, один скрипт для Мака и сервера. Вся запись в Озму — через ozma MCP (`transaction`, `run_action`), чтение — `funql_query` / `named_view_query`.

**Tech Stack:** Python 3 + faster-whisper 1.2.1 + ffmpeg; OzmaDB MCP; Notion MCP (рабочий воркспейс `c2755fd9…`).

**Spec:** `docs/superpowers/specs/2026-09-16-b2b-skill-design.md`

## Global Constraints

- Мастер-промпт — только в Notion, в git не копировать (репо публичный).
- Промпт `/b2b` ссылается на правила Дамблдора (Notion `3db612c762af81fbb9bdd4c06aaacf59`) для шагов/закрытия/шагов 8–9, не пересказывает их.
- Создание клиента, сделки, продукта; смена аккаунта/ответственного/клиента у сделки — только после «да» на карточку-резюме.
- Ничего не удалять (только `is_deleted = true` и только после «да»).
- Даты — по Москве; в базу писать с `+03:00`.
- Аккаунты: Сучкова 11061, Вавилина 11362, Гусакова 15832, Ромашко 28, Старцев 29.
- Модель расшифровки: `small`, `compute_type=int8`, язык `ru`.

---

### Task 1: Скрипт расшифровки аудио

**Files:**
- Create: `scripts/transcribe.py`
- Test: `scripts/test_transcribe.py`

**Interfaces:**
- Produces: CLI `python3 scripts/transcribe.py <audio> [--model small]` → текст в stdout (одна строка на сегмент склеена пробелом), код 0; ошибка → сообщение в stderr, код 1. Функция `transcribe(path: str, model: str = "small") -> str`.

- [ ] **Step 1: Тест.** Генерирует русскую речь голосом Milena через `say`, прогоняет `transcribe`, проверяет ключевые слова.

```python
import os, subprocess, tempfile, pytest
import importlib.util

HERE = os.path.dirname(os.path.abspath(__file__))
spec = importlib.util.spec_from_file_location("transcribe", os.path.join(HERE, "transcribe.py"))
transcribe = importlib.util.module_from_spec(spec)
spec.loader.exec_module(transcribe)


@pytest.mark.skipif(subprocess.call(["which", "say"], stdout=subprocess.DEVNULL) != 0, reason="нужен macOS say")
def test_transcribes_russian_speech():
    with tempfile.TemporaryDirectory() as d:
        aiff = os.path.join(d, "v.aiff")
        subprocess.run(["say", "-v", "Milena", "-o", aiff,
                        "Привет, это Вика. Созвонилась со Сбером, перезвонить в четверг."], check=True)
        text = transcribe.transcribe(aiff).lower()
    assert "вика" in text
    assert "сбер" in text
    assert "четверг" in text


def test_missing_file_exits_1():
    r = subprocess.run(["python3", os.path.join(HERE, "transcribe.py"), "/nope.ogg"],
                       capture_output=True, text=True)
    assert r.returncode == 1
    assert "не найден" in r.stderr
```

- [ ] **Step 2:** `python3 -m pytest scripts/test_transcribe.py -v` → FAIL (нет `transcribe.py`).

- [ ] **Step 3: Реализация.**

```python
#!/usr/bin/env python3
"""Расшифровка голосового в текст (faster-whisper, русский).

Использование:
    python3 scripts/transcribe.py <файл> [--model small]

Форматы — всё, что читает ffmpeg (ogg/oga из Telegram, m4a, mp3, wav, aiff).
Модель по умолчанию small/int8: короткое голосовое за секунды на CPU.
Используется скиллом /b2b в Claude и ботом Дамблдором на сервере.
"""
import argparse
import os
import sys


def transcribe(path: str, model: str = "small") -> str:
    from faster_whisper import WhisperModel
    m = WhisperModel(model, device="cpu", compute_type="int8")
    segments, _ = m.transcribe(path, language="ru", vad_filter=True)
    return " ".join(s.text.strip() for s in segments).strip()


def main():
    p = argparse.ArgumentParser(description="Расшифровка голосового в текст")
    p.add_argument("path")
    p.add_argument("--model", default="small")
    a = p.parse_args()
    if not os.path.isfile(a.path):
        print(f"Файл не найден: {a.path}", file=sys.stderr)
        return 1
    print(transcribe(a.path, a.model))
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4:** `python3 -m pytest scripts/test_transcribe.py -v` → 2 passed.

- [ ] **Step 5: Commit** `scripts/transcribe.py scripts/test_transcribe.py` — «scripts/transcribe.py: расшифровка голосовых для /b2b».

---

### Task 2: Мастер-промпт в Notion

**Files:** Notion — новая страница «🤖 Мастер-промпт: /b2b — корпоративные клиенты», родитель «База знаний» `6f0c0859c8ab46e2838107aeb3b4726c`.

**Interfaces:**
- Consumes: `scripts/transcribe.py` (Task 1).
- Produces: id страницы Notion — для `SKILL.md` (Task 3) и перекрёстной ссылки в промпте Дамблдора.

- [ ] **Step 1: Проверить, что данные в разделах ниже актуальны** — перед написанием перечитать `list_entity_fields` для `crm.b2b_clients`, `crm.b2b_deals`, `crm.b2b_client_contacts`, `crm.b2b_client_organizations`, `crm.b2b_deal_directions`, `crm.b2b_deal_actions` и справочники (источники, направления, типы, аудитории, бренды, статусы).

- [ ] **Step 2: Создать страницу.** Разделы (в том же стиле, что у Дамблдора — вводная цитата, «Железные правила», модель, запросы, форматы):
  1. **Кто такой /b2b** — руки раздела «Корпоративные клиенты»; работает в Claude и в Дамблдоре; шаги сделки — промпт Дамблдора (ссылка).
  2. **🔒 Железные правила** — confirm-first-список из Global Constraints; не угадывать; поиск дублей; голос → только через карточку; после записи перечитать; ничего не удалять; даты МСК.
  3. **Кто говорит** — таблица аккаунтов (имя, варианты обращения «Вика/Виктория/Вавилина», id); приоритет: фраза → `integration.b2b_bot_users.telegram_chat_id` → тема форума → не задан; что меняет контекст; сводка после «это Вика» (запросы 1–2 Дамблдора без условия «через день», по этому человеку).
  4. **Модель данных** — поля клиента, сделки, контакта, юрлиц, направлений, действий (списком, как у Дамблдора); справочники с id: источники 1–9, направления 1–11, типы продукта 1–6, аудитории 1–3, бренды (запросом, пополняется), формат `Очно`/`Онлайн`.
  5. **Как заводить** — по сущностям:
     - клиент: поиск дублей `SELECT c.id, c.main_name, c.brand=>name FROM crm.b2b_clients AS c WHERE NOT c.is_deleted AND (c.name ILIKE '%…%' OR c.team ILIKE '%…%' OR c.brand=>name ILIKE '%…%')`; нет бренда — вставить в `crm.b2b_brands` в той же транзакции; юрлицо — искать в `base.organizations` по ИНН/названию, связь `crm.b2b_client_organizations`; `account_manager` = человек из контекста;
     - контакт: дубли по phone/email/telegram;
     - сделка: `status` 1, `request_date` сегодня, `responsible` = контекст, направления отдельными строками `crm.b2b_deal_directions`; после — проверить, что появился шаг 2;
     - продукт: только `run_action crm.create_b2b_product_from_deal { deal }`;
     - заявка с сайта (`source` 9, клиент создан интеграцией из имени человека): найти настоящего клиента или завести; перевесить `deal.client`; назначить `responsible`; старого клиента-«человека» пометить `is_deleted` — всё одним резюме и «да»;
     - действие: insert `crm.b2b_deal_actions` без `stage`, `action_status` «Запланировано», `action_datetime` `YYYY-MM-DDT10:00:00+03:00` по умолчанию;
     - инфа: update только названных полей (ретеншн клиента, `notes`, контакт, `materials_link`, `client_request`, `our_understanding_note`, `result` действия).
  6. **Формат карточки-резюме** — пример для клиента+сделки из голосового, 4–6 строк, «Подтверди «да»».
  7. **Отчёты** — таблица «вопрос → вью/запрос»: 8 пунктов меню (`b2b_site_requests_table`, `b2b_clients_table`, `b2b_deals_table`, `b2b_products_table`, `b2b_clients_to_return`, `b2b_whats_hot`, `b2b_organizations_coverage`, `integration.b2b_bot_users`), карточка клиента (запрос: сделки, продукты с `sum(price)`, действия по всем сделкам, ретеншн), воронка по статусам с фильтром по человеку.
  8. **Голос** — Claude: `python3 scripts/transcribe.py <файл>`; бот: скачать voice, тот же скрипт; сначала ответить «Услышал: …», потом действовать по тексту.
  9. **💬 Как писать** — как у Дамблдора: русский, коротко, жирным клиенты/сделки, без SQL и полей людям; в Telegram без таблиц, в Claude таблицы можно.

- [ ] **Step 3:** В промпте Дамблдора в правиле 5 заменить «(это `/b2b`)» на ссылку на новую страницу.

- [ ] **Step 4:** `notion-fetch` новой страницы → убедиться, что таблицы и код-блоки отрендерились, `truncated` нет.

---

### Task 3: SKILL.md

**Files:**
- Create: `.claude/skills/b2b/SKILL.md`

**Interfaces:**
- Consumes: id страницы из Task 2.

- [ ] **Step 1:** Написать по образцу `.claude/skills/b2b-deals/SKILL.md`:

```markdown
---
name: b2b
description: Раздел «Корпоративные клиенты» OzmaDB — заводит B2B-клиентов, сделки, продукты из сделки, действия, дописывает информацию (ретеншн, контакты, итоги разговоров), разбирает заявки с b2b-сайта и отвечает по отчётам раздела (клиенты, воронка, продукты, ретеншн, ближайшие действия, охват по брендам). Понимает голосовые (расшифровка scripts/transcribe.py). Фраза «Привет, это Вика / Саша / Катя / Илья / Женя» переключает разговор на этого аккаунта и его клиентов. Создание и смена ответственных — только после «да». Подтягивает актуальный мастер-промпт из Notion и работает по нему. Используй, когда говорят «это Вика», «заведи клиента», «новая сделка», «добавь контакт», «запиши итог звонка», «напомни позвонить», «что у меня горит», «мои клиенты», «сколько сделок на КП», «что продали Сберу», присылают голосовое про B2B — даже если слово «скилл» не звучит. НЕ отмечает шаги цепочки и не закрывает сделки (это /b2b-deals, Дамблдор), НЕ трогает договоры и экономику.
---

# /b2b — Корпоративные клиенты

Руки раздела «Корпоративные клиенты» OzmaDB — в Claude и в Telegram-боте Дамблдоре (@gs_b2b_bot).

## Как работает

1. **Загрузи актуальный мастер-промпт из Notion:** page `<ID из Task 2>` (https://www.notion.so/<ID>). Действуй строго по нему.
2. Шаги сделки, «КП на доработке», закрытие, шаги 8–9 — по промпту Дамблдора `3db612c762af81fbb9bdd4c06aaacf59`.
3. Пришёл аудиофайл — сначала `python3 scripts/transcribe.py <файл>`, покажи «Услышал: …», дальше как с текстом.
4. Не работай по памяти. Если Notion недоступен — ничего не записывай в Озму, скажи об этом одной строкой.
```

- [ ] **Step 2:** В `.claude/skills/b2b-deals/SKILL.md` не менять ничего (там уже «это /b2b»).
- [ ] **Step 3: Commit** `.claude/skills/b2b/SKILL.md` — «Скилл /b2b — корпоративные клиенты».

---

### Task 4: Проверка на живой Озме

Прогон в Claude по сценариям спеки §9, каждый — строго по промпту из Notion.

- [ ] **Step 1:** «Привет, это Вика» → сводка по 11362; «мои клиенты» → только `account_manager = 11362`. Сверить с `funql_query`.
- [ ] **Step 2:** `say -v Milena -o /tmp/…/deal.aiff "Это Вика. Новый клиент ТЕСТ бэ-два-бэ, команда проверки, хотят стратсессию на двадцать человек, очно"` → transcribe → карточка-резюме. **Остановиться, показать Бэлле, ждать «да».**
- [ ] **Step 3:** После «да»: клиент и сделка созданы, у сделки появился шаг 2 (`crm.b2b_deal_actions WHERE deal = …`), ответственный 11362.
- [ ] **Step 4:** Повторить заведение «ТЕСТ» → скилл находит дубль, не создаёт.
- [ ] **Step 5:** Ручное действие «напомни позвонить в пятницу», итог разговора, ретеншн — записаны сразу, перечитаны.
- [ ] **Step 6:** Отчёты: воронка Вики, ретеншн, карточка клиента «ТЕСТ».
- [ ] **Step 7:** С «да» Бэллы пометить тестовые клиент/сделку `is_deleted = true`, открытые шаги — «Отменено».
- [ ] **Step 8:** Найденные расхождения промпта с поведением — поправить страницу Notion.

---

### Task 5: Расшифровка на сервере

Готовим для будущего бота; сам бот не выкладываем.

- [ ] **Step 1:** `ssh root@ozma.gogol.school 'apt-get install -y ffmpeg'`.
- [ ] **Step 2:** Под `agentbot`: `pip3 install --user faster-whisper==1.2.1` с `HTTPS_PROXY=http://127.0.0.1:1087` (загрузка модели с Hugging Face тоже через прокси).
- [ ] **Step 3:** `git -C /home/agentbot/gogol-school-ai pull` — чтобы появился `scripts/transcribe.py` (после push ветки — спросить Бэллу).
- [ ] **Step 4:** Смоук: скопировать тестовый `.aiff` → `/usr/bin/time -v python3 scripts/transcribe.py v.aiff` → текст с «Вика», пиковая память (`Maximum resident set size`) < 1,5 ГБ, время < 30 с.

---

### Task 6: Память и итог

- [ ] **Step 1:** Memory-файл `reference_b2b_skill.md`: где промпт, что делит с Дамблдором, расшифровка (Мак / сервер), что не сделано (подключение к боту). Строка в `MEMORY.md`.
- [ ] **Step 2:** Обновить `reference_dumbledore_b2b_bot.md`: боту грузить оба промпта, голос через `scripts/transcribe.py`.
