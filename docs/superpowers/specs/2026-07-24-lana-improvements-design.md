# Лана: автоцикл без подтверждений, сводка в 12:00, модули, вёрстка приписки

Дата: 24.07.2026. Заказчик: Бэлла (заметки от 24.07). Исполняется по фидбэку после ~2 недель работы Ланы в чате «NEW GS Возвраты» (-1003933036305).

## Что меняем (5 пунктов Бэллы)

1. Убрать уведомления о письмах — новое письмо обрабатывается сразу и молча (черновик, Notion, расчёт, статусы в Озме).
2. В 12:00 сводка: новые созданные кейсы (ФИО + почта).
3. В 12:00 сводка по срокам: красные/жёлтые дедлайны.
4. Путаница в сумме (кейс Лебедевой, ЛАБ-2 на 16 занятий): модули сверять по описанию продукта в Озме и по /schedule/ на сайте; расхождение — сообщать, не решать самой.
5. Форматирование черновиков: нижняя приписка про сертификаты разваливается.

## Где что живёт

- Бот: `/home/agentbot/bots/lana/bot.py` на ozma (systemd `lana-bot`, user agentbot). Промпты оркестратора (`ORCH_MAIL_PROMPT`, `ORCH_DIGEST_PROMPT`), системный промпт (`PERSONA`), allowlist тулов, планировщик — всё там. Правится на сервере напрямую + `systemctl restart lana-bot`.
- Скилл: `roles/client-office-ops/skills/refund-request/SKILL.md` в этом репо; на сервер попадает через git pull клона `/home/agentbot/gogol-school-ai`.
- Модель: сейчас нигде не задана → CLI-дефолт = Sonnet 4.6. Переход на Opus — опция `--model` в `cmd` в bot.py (решение Бэллы, отдельно).

## 1. Тишина + полный автоцикл

**bot.py, `ORCH_MAIL_PROMPT`** — переписать:
- Новое письмо (типовой кейс) → сразу весь цикл по скиллу: статусы в Озме («Отмена» + «Попросил возврат»), строка в Notion, расчёт сумм, черновик ответа в Drafts (reply в цепочку), `mark_read`. В чат — ничего; в конце прогона ответ строго `NOOP`.
- Нестандартный кейс (список в скилле: спор о сумме, отмена школой, не нашла студента / несколько кандидатов, расхождение расчёта — см. §3, реквизиты письмом без анкеты и т.п.) → **никаких записей**, письмо остаётся непрочитанным; кейс всплывёт в сводке 12:00 блоком «нужно решение».
- Идемпотентность (важно: подтверждающего человека в петле больше нет): перед записью проверять, что уже сделано — есть ли нетерминальная строка в Notion по почте, стоят ли уже статусы в заявке, есть ли черновик в цепочке. Делать только недостающие шаги, дублей не плодить. `mark_read` — только после полного успешного цикла; если упёрлась в ошибку на полпути — письмо оставить непрочитанным и вынести в «нужно решение».

**bot.py, `PERSONA`, железное правило №2** — смягчить: подтверждение «да» в чате больше НЕ требуется для стандартного конвейера входящего письма о возврате (статусы, строка Notion, черновик). Остаётся обязательным для: всего, что просят в чате руками; скилла /refund (обратные транзакции); нестандартных кейсов; любых правок вне конвейера. Терминальный статус «деньги возвращены» — по-прежнему никогда. Железное правило №1 (только черновики, ничего не отправлять) — без изменений: контроль человека переезжает с «да» в чате на проверку черновика в Drafts перед отправкой.

**Скилл, шаги 5–6**: пометить, что в автономном прогоне (плановый разбор почты) шаг 5 «сводка и подтверждение» пропускается — типовой кейс исполняется по шагу 6 сразу; «текст письма показать оператору до создания черновика» в этом режиме не действует. В интерактивном режиме (человек попросил в чате/локально) прежний порядок сохраняется.

## 2. Сводка в 12:00

**bot.py**: `ORCH_DIGEST_HOUR = 10` → `12`; `ORCH_DIGEST_PROMPT` переписать. Три блока (каждый отдельным сообщением через `---`, пустые блоки опускать; если все пусты — `NOOP`):

1. **Новые созданные** — кейсы, оформленные с прошлой сводки (по `createdTime` строк Notion; в понедельник — с пятницы): ФИО + почта, программа, сумма, ссылка на строку Notion. Приписка «черновики в Drafts — проверить и отправить».
2. **Сроки** — по полю «Дедлайн» нетерминальных строк: 🔴 дедлайн прошёл или сегодня, 🟡 ближайшие (формат, который Лана уже делает: ФИО, почта, статус, дата). Плюс зависшие по SLA без дедлайна.
3. **Нужно решение** — отложенный нестандарт из почасовых прогонов (по непрочитанным письмам + флагам), каждый с коротким описанием, что именно решить.

## 3. Модули: три источника, расхождение — сообщать

Скилл, шаг 4, обязательный порядок для расчёта после старта (и сертификата по модулям):

1. **Число занятий и «прошло»** — по дочерним занятиям продукта в Озме (даты занятий vs дата письма).
2. **Границы модулей** — сверить три источника:
   - описание продукта в карточке Озмы (поле описания в `crm.actions` — там модули прописываются при заведении);
   - страница расписания сайта — Bitrix iblock 38, элемент 27918 (читать `iblock_element_get`; искать программу по коду/названию);
   - общая сетка из скилла (лаба 24: 4/4/16; >8 занятий: 2/2/ост.; ≤8: 1/2/ост.) — только как проверка/fallback.
3. Все три сходятся → считать; свой результат сверить с `auto_refund_amount` карточки.
4. **Любое расхождение** (источники между собой, или расчёт ≠ auto_refund_amount, или программы нет ни в описании, ни на /schedule/) → кейс нестандартный: черновик с суммой НЕ создавать, в сводку 12:00 «нужно решение» с цифрами по каждому источнику. Правило «не выдумывать М1/М2» (анти-кейс Лебедевой: ЛАБ-2 = 16 занятий, а не сетка 24) — прописать явно.

**bot.py, allowlist**: добавить `mcp__gogol-school-site__iblock_element_get` (read-only).

## 4. Вёрстка приписки про сертификаты

Скилл, шаг 6 (черновик):
- Блок «Правила частичного возврата» / «Правила перевода в сертификат» и сноску `*Использование сертификата…` брать из шаблона письма как есть, НЕ пересобирать текст.
- Требования к результату: сноска — отдельным абзацем после пустой строки; три следствия («присвоению статуса…», «прекращению обязательств…», «признанию денежных средств…») — маркированным списком, каждый пункт с новой строки; никаких `&nbsp;` и слипшихся абзацев в итоговом теле.
- При имплементации проверить, поддерживает ли `create_draft` HTML-тело: если да — вставлять HTML-блок шаблона; если только plain text — фиксированный plain-text вариант блока с явными переносами (канонический текст положить в скилл).

## Деплой и проверка

1. Скилл: правка в репо → commit/push → `git pull` в `/home/agentbot/gogol-school-ai` на ozma.
2. bot.py: правка на сервере (бэкап рядом `bot.py.bak-YYYYMMDD`) → `systemctl restart lana-bot` → смотреть `journalctl -u lana-bot -f` на первом плановом прогоне.
3. Приёмка: (а) тестовое письмо → полный цикл без сообщений в чат, черновик с нормальной припиской; (б) сводка в 12:00 с тремя блоками; (в) кейс с нестандартной программой → не считает сама, выносит в «нужно решение».

## Вне скоупа

- Переход Ланы на Opus (ждёт решения Бэллы).
- Автоотправка писем, /new-refunds-автозапуск, генерация заявлений — фазы плана «Лана Рефанд», не трогаем.
